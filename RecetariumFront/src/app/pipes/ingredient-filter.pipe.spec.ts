import { IngredientFilterPipe } from './ingredient-filter.pipe';

describe('IngredientFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
